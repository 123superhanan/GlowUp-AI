import { StyleSheet, Text, View } from "react-native";

const mode = () => {
  return (
    <View>
      <Text>mode</Text>
    </View>
  );
};

export default mode;

const styles = StyleSheet.create({});
